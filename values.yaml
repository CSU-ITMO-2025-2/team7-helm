replicaCount: 2

image:
  repository: buldakovn/flask-app
  tag: "latest"
  pullPolicy: Always

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: true
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 5000

# Startup Probe - проверяет, что приложение запустилось
# Используется для приложений с медленным стартом
startupProbe:
  enabled: true
  path: /health/startup
  initialDelaySeconds: 0
  periodSeconds: 10
  timeoutSeconds: 3
  failureThreshold: 30  # Максимум 5 минут на запуск (30 * 10 секунд)

# Liveness Probe - проверяет, что приложение все еще работает
# Если проверка не проходит, Kubernetes перезапускает контейнер
livenessProbe:
  enabled: true
  path: /health/live
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

# Readiness Probe - проверяет, готово ли приложение принимать трафик
# Если проверка не проходит, под удаляется из Service endpoints
readinessProbe:
  enabled: true
  path: /health/ready
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3


ingress:
  enabled: true
  host: team7.kubepractice.ru
  className: nginx
  annotations: {}
  tls:
    enabled: false
    secretName: tls-secret

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}

# External Secret configuration для интеграции с Vault
# Требуется установленный External Secrets Operator в кластере
# И настроенный SecretStore/ClusterSecretStore для подключения к Vault
externalSecret:
  enabled: true
  refreshInterval: 1h  # Как часто обновлять секрет из Vault
  secretStore:
    name: vault-team7  # Имя SecretStore или ClusterSecretStore (должен быть создан отдельно)
    kind: SecretStore  # SecretStore (namespace-scoped) или ClusterSecretStore (cluster-scoped)
  # Маппинг секретов из Vault в Kubernetes Secret
  # Каждый элемент определяет, какой ключ из Vault в какой ключ Secret записать
  data:
    # Вариант 1: Получить конкретное свойство из секрета
    - secretKey: HELLO_MESSAGE  # Имя ключа в Kubernetes Secret (будет переменной окружения)
      remoteRef:
        key: kv/data/team7/test  # Путь к секрету в Vault (для KV v1) или secret/data/hello (для KV v2)
        property: TEST_MESSAGE  # Конкретное свойство из секрета (опционально)
    # Вариант 2: Получить весь секрет целиком (если property не указан)
    # - secretKey: VAULT_SECRET_DATA
    #   remoteRef:
    #     key: secret/hello
    # Можно добавить больше секретов:
    # - secretKey: ANOTHER_SECRET
    #   remoteRef:
    #     key: secret/another
    #     property: value

